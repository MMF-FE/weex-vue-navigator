import * as Router from 'vue-router/types';
import Navigator from './navigator';
declare var _default: {
    install(VueClass: any, options: {
        router: Router;
    }): void;
    bootstrap(): void;
    Navigator: typeof Navigator;
};
export default _default;
